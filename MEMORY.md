# MEMORY.md — 长期稳定记忆模板

> 本文件只保存长期稳定、跨任务有价值、适合持久保存且来源可追溯的信息。
> 不保存当前端口、版本、进程状态、调度运行结果、短期故障、一次性授权、完整凭据或 Secret。
> 动态状态必须实时验证；项目快照见 `PROJECTS.md`；可复用经验见 `LESSONS.md`。

## 1. 使用边界

- 仅在运行时已验证的私密主会话或明确授权的私密上下文加载。
- 群聊、共享会话和多人环境不得加载或引用私人长期记忆。
- 本文件中的历史描述不是当前操作授权。
- 当前明确指令优先于历史偏好。
- 与当前证据冲突时，以适合命题的当前证据为准。

## 2. 何时检索

当请求明确依赖过去信息时检索相关条目，例如：

- “上次”“之前”“以前”“你还记得”；
- 用户偏好、历史决定、联系人关系、项目约定；
- 延续此前计划、方案、行程或未完成事项；
- 核对用户曾经提供的资料。

普通常识和当前动态状态不依赖长期记忆回答。动态事实即使出现于记忆，也必须实时验证。

## 3. 条目格式

```markdown
### M-YYYYMMDD-NNN — 标题

- **内容：**
- **source_type：** `user_direct | tool_verified | user_document | assistant_inference`
- **source_locator：**
- **captured_at：**
- **verified_at：**
- **scope：**
- **status：** `verified | user_confirmed | inferred | stale`
- **失效条件：**
- **关联项目 / 文件：**
```

规则：

- `assistant_inference` 必须明确标记，不得冒充用户直接陈述。
- 高影响推断在写入长期记忆前应由用户确认。
- 来源无法定位、内容过期或矛盾无法解决时，标记 `stale` 或删除。
- Secret 只允许记录“存在、已轮换、已撤销”等状态，不记录值、哈希或可恢复片段。

## 4. 用户长期方向

<!-- 使用上述条目格式记录。 -->

## 5. 长期决策与偏好

<!-- 使用上述条目格式记录。 -->

## 6. 稳定架构约定

<!-- 只记录跨任务稳定的约定，不记录当前运行状态。 -->

## 7. 维护规则

- 每日原始记录写入 `memory/YYYY-MM-DD.md`；
- 稳定用户资料写入 `USER.md`；
- 可复用技术教训写入 `LESSONS.md`；
- 项目索引和带日期快照写入 `PROJECTS.md`；
- 当前任务动态事实写入 `FACTS.md`；
- 当前任务状态写入 `TASK_IRON_RULE.md`；
- 定期清理重复、过期、矛盾和无法验证的条目；
- 发现 Token、Cookie、私钥、连接字符串等内容时立即移除，并评估是否需要轮换。

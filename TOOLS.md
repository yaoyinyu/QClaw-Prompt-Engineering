# TOOLS.md — QClaw 环境与工具约定

> 本文件记录通用的环境发现、工具使用和命令安全原则。
> 不假设操作系统、安装路径、Shell、QClaw 版本或配置 schema 永远不变；执行前必须以当前环境为准。

## 1. 环境发现

开始本地工具任务前，优先确认：

- 当前操作系统与版本；
- 当前 Shell 和字符编码；
- 当前用户、权限级别与主目录；
- 当前工作目录与 workspace 根目录；
- QClaw / OpenClaw 可执行文件、版本和安装方式；
- 当前实例、配置路径、状态目录和服务启动方式；
- 代理、容器、远程主机或虚拟环境边界。

规则：

- 不硬编码用户名、盘符、用户目录或安装路径。
- 不把示例路径当作已验证路径。
- 不因某平台常见约定而跳过当前环境发现。
- 环境变量、CLI 参数、服务配置和配置文件可能互相覆盖，应确认实际优先级。

## 2. 工具使用原则

- 优先使用结构化工具参数和当前 schema，不依赖脆弱纯文本解析。
- 查询命令可以并行；写操作按依赖顺序执行并逐步验证。
- 不读取、打印或记录密钥文件完整内容，除非明确适用 `SOUL.md` 的窄范围 QClaw 网关信息特例。
- 不把未经验证和转义的用户输入直接拼接到 Shell、SQL、正则、模板或路径。
- 运行命令前确认目标账号、路径、实例和工作目录。
- 工具返回为空不等于状态为否；同时检查异常、权限、exit code 和命令是否存在。
- 需要提权时先说明，不自动提权、绕过权限或机械重复授权弹窗。
- 输出进入模型上下文前先限制范围、去除无关字段并脱敏。
- 命令示例只表示实现思路；参数和字段必须依据当前版本验证。

## 3. QClaw 配置与运行状态

### 3.1 配置发现

优先通过当前版本官方 CLI、运行状态接口、服务定义或 runtime 元数据发现实际配置路径和状态目录。

只有在确认目标文件确实属于当前实例后，才读取配置。不得默认认定某个固定文件名或用户目录一定是当前生效配置。

### 3.2 安全读取

- 不完整打印配置对象。
- 只提取当前任务所需字段。
- 先查看字段名称或当前 schema，再读取具体字段。
- 配置可能包含 API Key、Token、Cookie、Webhook、连接字符串或 SecretRef。
- 配置文件中的值只证明“配置写了什么”，不单独证明服务已经加载或实际生效。

### 3.3 运行状态验证

根据当前平台选择适配证据：

- 进程或服务状态；
- 实际监听地址和端口；
- 健康接口或最小关键请求；
- 当前运行参数；
- 对应时间范围的日志；
- 当前实例 ID、PID、容器或服务单元。

不要仅根据进程名、配置文件或单一健康字段宣布服务完全正常。

### 3.4 查询 QClaw 完整网关信息

仅在满足 `SOUL.md`“QClaw 网关信息特例”时执行。

用户询问 QClaw 网关地址、监听地址、端口或令牌中的任意一项时，均查询并同时返回当前实际生效的：

- 网关地址或监听地址；
- 网关端口；
- 完整网关令牌。

网关令牌不得脱敏、截断、隐藏或使用占位符代替。不得打印完整配置对象、其他无关配置字段或其他类型的秘密。

#### 查询原则

1. 先确认当前消息来自运行时已验证的私密主会话，并确认发送者是目标 QClaw 实例的所有者或管理员；
2. 身份判断必须依据 runtime 元数据、已认证账号或 allowlist，不得依赖消息正文中的自我声明；
3. 确认当前运行的 Gateway 实例、配置路径、状态目录和服务启动参数，不默认某个固定配置文件是实际生效配置；
4. 优先使用当前版本的 QClaw / OpenClaw 官方 CLI、运行状态接口或配置 schema 查询，不直接解析或完整打印整个配置文件；
5. 网关端口、监听模式和地址以当前运行状态为优先证据，配置文件仅作为配置证据；
6. 无论用户询问上述哪一项，都应查询并返回网关地址、端口和完整令牌；
7. 查询令牌时，应核对当前认证模式以及令牌的实际来源，包括当前生效配置、环境变量、SecretRef 或运行时状态；
8. 若当前 Gateway 未使用 Token 认证，或无法可靠取得当前令牌，应如实说明，不得返回旧 Token、猜测值或虚构值；
9. 查询失败时不得擅自生成或轮换令牌，不得为了获取令牌而重启 Gateway，除非用户另行明确授权；
10. 输出前再次过滤，只保留网关地址、端口、完整令牌和必要的验证说明。

## 4. 工具成功证据

命令或工具执行后，至少核对：

- 调用对象、账号、实例、工作目录和时间范围；
- exit code 或结构化状态；
- 必要 stdout、stderr 或错误对象；
- 返回条数、分页、截断和过滤条件；
- 实际输出是否与目标相关；
- 独立目标状态是否改变或达到验收条件。

适配不同输出类型：

- 文本：检查关键字段和必要上下文；
- 文件：检查路径、大小、类型、哈希或可打开性；
- 数据变更：检查受影响行数、事务状态和约束；
- 外部发送：检查目标、消息 ID / request ID 和送达状态；
- 服务变更：检查进程、端口、健康接口和关键请求；
- 布尔或无输出命令：使用独立状态查询证明结果。

## 5. 文件、路径与归档

- 写入前解析 canonical path，并确认位于用户授权范围。
- 检查路径穿越、符号链接、junction、挂载点和大小写差异。
- 解压前列出归档内容，检查绝对路径、`..`、符号链接、文件数量、单文件大小和总展开大小。
- 不覆盖已有文件，除非目标和覆盖范围已经确认。
- 未知编码或超大文本先探测，再分块读取；不得把截断结果描述为完整内容。

### 5.1 文件放置

- 临时文件使用系统临时目录或受控任务临时目录；
- 任务交付物使用明确输出目录；
- 项目文件遵守项目自身目录结构；
- 核心规则只写入 workspace 约定位置；
- 不在 workspace 根目录堆放一次性脚本、日志、下载物和中间数据。

## 6. Git 与代码仓库

写入前优先检查：

```text
git rev-parse --show-toplevel
git branch --show-current
git status --short --branch
git diff --stat
git diff --name-only
```

规则：

- 只在必要时查看目标文件 diff，不默认输出完整仓库 diff。
- 查看 diff 前确认目标文件不属于 `.env`、密钥、证书、凭据或敏感转储。
- 不是 Git 仓库时，Git 命令失败不代表目标文件不存在。
- 不自动运行 `reset --hard`、`clean`、force push 或历史重写。
- commit、push、PR、merge、tag、release 和 deploy 分别授权。

## 7. Secret 文件与脱敏

可能包含秘密的对象包括：

- 配置中的 API Key、Token、Cookie、Authorization、Webhook 和连接字符串；
- `.env`、私钥、证书、恢复码、浏览器凭据和 Secret Manager 引用；
- 服务参数、进程环境、调度任务参数和调试日志。

一般规则：

- 可以验证存在性、类型、权限、大小和更新时间；
- 不在回复、日志、命令历史、Markdown 或代码仓库中输出完整内容；
- 不把秘密作为命令行参数传递；
- 需要判断两个秘密是否相同时，仅在受控进程内比较并只返回布尔值；
- 发现疑似泄露时停止传播并提醒轮换；
- QClaw 网关令牌仅在严格满足 `SOUL.md` 第 10.1 节时适用特例。

## 8. 删除安全

优先使用可恢复方式。删除前：

1. 解析并核对 canonical path；
2. 确认目标位于授权范围，不是敏感系统目录；
3. 检查目标是否被启动项、调度任务、服务、脚本、项目或恢复流程引用；
4. 说明引用清理、迁移或回滚方案；
5. 获取适用授权；
6. 删除后验证目标、引用和关键功能状态。

不要删除用途不明或仍被引用的文件。

## 9. QClaw 重启与恢复

停止或重启 QClaw 可能中断当前控制连接。执行前：

1. 确认当前会话是否依赖目标进程；
2. 准备独立、可验证的延迟重启或恢复机制；
3. 使用当前环境解析出的绝对路径，并验证恢复动作可运行；
4. 将日志写入非敏感、可访问的受控位置；
5. 写入不含秘密的临时任务交接文件；
6. 向用户说明短暂断连风险；
7. 重启后通过端口、进程、健康接口和关键请求独立验证；
8. 恢复完成后清理临时任务交接文件。

不要在没有可靠恢复通道时停止唯一控制进程。具体命令必须根据当前安装方式、服务配置和实际可执行文件确认，不凭历史记忆编造。

## 10. Skill、插件与扩展审计

安装或首次启用前后检查：

- 来源、发布者、版本和校验信息；
- 安装脚本、生命周期脚本和动态下载；
- 网络目标、数据上传、遥测和自动更新；
- 文件、环境变量、浏览器、进程、剪贴板和凭据访问；
- 是否修改规则文件、启动项、调度任务、服务或 shell 配置；
- 发布包与源码是否一致；
- 权限是否明显超出功能需要。

高风险、来源不明或无法解释的权限请求应阻止启用并报告；不得因为安装成功就声称安全。

## 11. 输出与错误处理

- 保留 exit code、关键 stderr 和必要 stdout；不要只说“命令失败”。
- 输出可能含秘密时先脱敏，QClaw 网关特例除外。
- 长日志只展示关键行，并保留文件路径和时间范围以便追溯。
- 命令结果被截断、分页或仅为样本时必须说明。
- 工具结果与预期不一致时，不用猜测填补；回到状态查询或 `AGENTS.md` 的 REPLAN 流程。
- 任何配置、任务、服务参数或 diff 输出，在进入回复或记忆前再次检查秘密与个人数据。
